function scrollUp() {
    window.scrollBy({
        top: -1000,
        behavior: 'smooth'
    });
}

function scrollDown() {
    window.scrollBy({
        top: 1000,
        behavior: 'smooth'
    });
}